"//"    -   Integer division

Random -
    choice - item from a list of inputs

is vs ==
    - == checks values
    - is checks instaces, if something is at the same memory location
        - a = [1,2]
        - b = [1,2]
        - a is b -> false - not the same mem location (object)

        - a = [1,2]
        - b = a
        - b is a - True